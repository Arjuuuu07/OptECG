# OptECG
OptECG is a specialized computer vision and signal processing framework designed to transform  ECGs into standardized digital data using a camera. By prioritizing Explainable AI (XAI), it converts raw pixels into precise medical units (mV and seconds), enabling transparent, rule-based clinical diagnostics.
